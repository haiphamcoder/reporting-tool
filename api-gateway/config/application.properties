# Local configuration for API Gateway
server.port=8765
spring.application.name=api-gateway

# Eureka server URL (local)
eureka.client.serviceUrl.defaultZone=${DISCOVERY_SERVER_URL:http://localhost:8761}/eureka

# CORS: cho phép local development
gateway.cors.allowed-origins=${CORS_ALLOWED_ORIGINS}

# Logging level cho development
logging.level.root=INFO
logging.level.org.springframework.web=DEBUG
